$(document).ready(function(){
    /* 1.:enabled  \disabled
        匹配所有可用的元素  \  匹配所有不可用的元素
    */
    var inputs = $(':enabled');
    
   /*  2.:checked
        匹配所有选中的被选中的元素(复选框、单选框，select中的option等)
    */
   var texts =$(':checked');
   
   /* 3.selected
        匹配option中所有选中的被选中的元素
   */
  
})